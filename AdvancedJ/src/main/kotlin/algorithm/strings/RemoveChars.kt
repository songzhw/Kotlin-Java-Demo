package algorithm.strings

import java.util.*

fun remove(src: String, delete: String): String {
    val bits = BitSet()
    for (c in delete.toCharArray()) {

    }
    return ""
}

fun main() {
    val c = 'a'
    println(c.toInt())
}